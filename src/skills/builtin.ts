/**
 * Skills that ship with the agent.
 *
 * Deliberately stored as raw markdown and parsed by the same `parseFrontmatter`
 * that user directories go through, for the same reason the built-in experts
 * are: the file format is the one users actually write, so the built-ins and
 * the format must not be allowed to drift apart. If the parser breaks, the
 * built-ins break with it, which is the point.
 *
 * The ids here are the directory names the equivalent user skill would have.
 *
 * Kept to a handful. The value of a built-in catalog is the *mechanism*, not
 * the breadth — more skills arrive as user files, and the shipped set only
 * needs to prove the mechanism works and to cover a couple of tasks everyone
 * does.
 */

/** Raw markdown, exactly as a user's SKILL.md would be written. */
export const BUILTIN_SKILLS: ReadonlyArray<{ id: string; source: string }> = [
	{
		id: "git-commit",
		source: `---
name: 提交改动
description: 把改动整理成一条清晰的 commit，遵循项目自己的提交节奏
when_to_use: 用户说"提交"、"commit"、"推送"、或要求把一批改动整理成提交
---

目标是**一次提交表达一件事**，而不是把一堆零碎塞进一条。

先弄清楚改动的范围，再决定怎么切分：

1. **读改动**：git status、git diff，搞清楚改了哪些文件、分别是什么性质的改动。
2. **切分**：相互独立的东西分成多条提交。逻辑上属于一个改动的文件放一起。
3. **写提交信息**：一行标题说清"做了什么"（祈使句，现在时），需要时用正文说明
   "为什么"和取舍。正文解释动机，不重复标题。
4. **不要擅自提交**：除非用户明确说"提交吧"，否则整理好、报告改了哪些文件，
   等用户点头再提交推送。边做边 commit 不是这个项目要的节奏。
`,
	},
	{
		id: "write-readme",
		source: `---
name: 写 README
description: 面向"第一次打开的人"写一份能快速上手、不啰嗦的 README
when_to_use: 用户要新建、重写或改进项目的 README / 说明文档
---

README 的读者是"第一次打开这个仓库的人"，不是作者自己。

按这个结构写，按需省略不相关的节：

1. **一句话说清这是什么** —— 一段话，不绕弯子。
2. **快速开始** —— 最少的命令让人跑起来。命令要能直接复制，不要占位符。
3. **它能做什么** —— 能力清单，每条一句话。
4. **怎么配置** —— 用户必须自己填的东西（key、环境变量）要写清楚，且写明
   不填会怎样。
5. **项目结构** —— 目录一眼看懂，不是完整树状图。

原则：

- **给真命令，不给占位符。** 读者真的会照着敲，尖括号和结尾的波浪线都会被带进去。
- **说清"不 X 会怎样"**，不只说"X 是什么"。比如"不填 key 会话起不来"比"需要 key"有用。
- **错了就改，不硬凑长度。** 一句话能说清的写一句。
`,
	},
	{
		id: "skill-creator",
		source: `---
name: 写技能
description: 按"渐进式披露"的格式，把一套可复用流程写成 SKILL.md
when_to_use: 用户要新建、重写或完善一个技能 / SKILL.md，或问"怎么给这个 agent 加能力"
---

把一个流程固化成技能，本质是写一份"只有任务命中才被加载的方法论"。

先想清楚划分依据：

1. **它是流程，不是知识。** 一次性的答案不值得做技能；技能的价值在"每次遇到这类任务
   都能照着做，且比临场发挥好"。
2. **教程级但精炼。** 体例是"遇到这类任务怎么做"，不是"这是什么"的百科。

按这个格式写：

\`\`\`markdown
---
name: <显示名>
description: <一句话，模型据此决定是否加载这篇正文>
when_to_use: <触发短语，简短的一句话；不是每次都需要>
---

<body: 遇到这类任务时按什么步骤做，含取舍和为什么。>
\`\`\`

正文要点：

- **description 与 when_to_use 是广告，正文是货。** 模型在加载前只看到前两者，所以
  它们要能让人判断"这任务是否命中"。正文可以长，前两行必须准。
- **写"怎么做、为什么"。** 不只写结论，给出判断依据，模型才能在你没说到的边界上做对。
- **不重复常识。** 模型本就会的不用写；写的是比临场发挥更好的约定、顺序和取舍。

落盘位置：个人技能放 \`~/.gdou-agent/skills/<id>/SKILL.md\`，工作区技能放
\`<cwd>/.gdou-agent/skills/<id>/SKILL.md\`。目录名即技能 id，要稳定、唯一。
`,
	},
	{
		id: "find-skills",
		source: `---
name: 找技能
description: 从已装/内置技能里挑出适合当前任务的，给出命中理由，不替用户硬选
when_to_use: 用户给了一个任务，想先确认有没有现成技能可用；或技能很多不知道用哪个
---

一个技能对应一类任务。找技能的目的是"这个任务正好该用哪个"，不是"把所有技能都用上"。

1. **先读目录。** 列出当前可用的技能（id、一句话描述、触发条件），凭描述筛掉大部分。
2. **按 when_to_use 命中。** 逐条对用户任务，命中多个就按相关性排序，相关的那个才去
   读正文（load body / read reference）。
3. **主动加载，不甩给用户挑。** 命中就带正文边看边干；"可用但这里不该用"也要说清，
   免得用户以为技能没生效。
4. **没命中就直说。** 没有现成技能时不要生搬硬套，诚实告诉用户不在已装目录里；再判断
   是否值得新建一个技能来做（见「写技能」技能）。
`,
	},
	{
		id: "review-agent",
		source: `---
name: 代码审查
description: 以"上线后会不会出事"为最高优先，按 回归 → 数据 → 一致性 → 安全形态 的顺序审查
when_to_use: 用户要求 review / 审查 / code review 一批改动、commit 或 PR
---

审查目标不是挑毛病或夸代码，是"别让这改动上线后出事"。

按优先级从高到低：

1. **能不能跑、会不会回归（最高）。**
   - 改动是否破坏现有调用方：签名、默认值、返回值变化。
   - 有没有漏处理的分支：空值、边界、并发、取消。
   - 新逻辑是否悄悄改了旧行为——即使不报错。
2. **数据与状态。** 错误被吞了吗？资源（连接、句柄、定时器）会泄漏吗？写入原子的吗？
   失败重试会不会重复副作用？
3. **一致性与风格。** 命名、错误处理、返回形状是否和相邻代码一致——这不是阻塞项，
   别为风格重写过关的代码。
4. **安全相关的形态（只看本次改动引入的）。** 路径拼接、命令注入、敏感数据落盘。
   与本次改动无关的存量问题，确认后单独提，不混进 review。

按这种结构输出：
**阻塞项**（会回归 / 数据的坑）→ **建议项**（更好但不挡上线）→ **一致性备注**。
每条给 \`文件:行\`、问题、为什么、怎么改。以问题描述为主，不给完改法就算完。
`,
	},
	{
		id: "frontend-design",
		source: `---
name: 做前端
description: 用项目现有视觉语言和交互，实现状态完整、可交付的前端界面
when_to_use: 用户要求做页面、写组件、改前端 UI，或让它"好看一点、整齐一点"
---

前端改动默认**沿用项目现有风格**，不另起一套视觉体系。

动手前先读约定：

1. **读现有的设计语言。** 颜色、圆角、字号、间距、阴影怎么来的（CSS 变量还是硬编码），
   新组件复用同一套 token，别引入另一套。
2. **状态优先于颜值。** 先想交互状态（加载 / 空 / 错误 / 禁用 / 悬停 / 聚焦），再想好看。
   每个状态都该有可见表现——尤其"空"和"错误"，这决定界面是"能用"还是"像坏了"。
3. **语义用对元素。** 按钮用 button、弹层用 dialog、可选项标 role。能键盘到达、focus
   可见；别为了样式换成 div。
4. **响应式。** 能用 min/max-width 流式就别写死宽高，窄屏该折叠的折叠。

交付前自查：所有状态可见吗？能键盘操作吗？刷新不闪内容吗？没有占位假图吗？
`,
	},
	{
		id: "documents",
		source: `---
name: 写文档
description: 面向指定读者写正式文档：先定读者/目标/形式，再结论先行下笔
when_to_use: 用户要写方案、说明、交接文档、周报等正式文本
---

文档是写给"特定读者"用的，不是写给自己看的。

动笔前先定三件事：

1. **读者是谁。** 已知道什么、需要补什么、要用这文档干什么。
2. **目标是什么。** 读完能做什么 / 决定什么——一句话说清。
3. **形式。** 方案、说明、周报选不同结构和语气。

写作原则：

- **结论先行。** 标题和开头即要点，细节往下铺。
- **一段一个事。** 段落开头是这句主旨。
- **给具体的。** 命令、数字、日期、示例；不给"适当""合理"这类模糊词。
- **长文给结构。** 目录 / 分节 / 列表，让读者能跳着读。
- **别隔靴搔痒。** 能一句说清就一句，宁短勿水。

写完自检：读者找准了吗？一眼能看到要找的东西吗？有没有一段塞了一堆没说重点？
`,
	},
	{
		id: "presentations",
		source: `---
name: 做演示文稿
description: 把要点整理成结构清楚、适合现场讲解的演示稿，一页只讲一件事
when_to_use: 用户要做 PPT、演示、讲解用的幻灯片，或要"做一份讲稿"
---

演示稿是讲的人用来引导现场的提示卡，不是文档搬上去。

1. **先想故事线。** 背景 → 问题 → 方案 → 论据 → 下一步；一页一个论点。
2. **每页一句话。** 页面标题就是这页要传达的唯一要点；放不下说明这页塞太多。
3. **能删的都删。** 讲稿能说的不必全打在页面上；比例 / 图表 / 结论留给口头。
4. **按目标格式落地。** 目标是 markdown 幻灯片（如 Marp）就用对应分隔/语法排版；
   有现成模板就遵循它，别自创一套既定的版式。

完工自检：不看备注能否讲下来？每页是不是只讲一件事？图 / 表有没有一句话结论？
`,
		},
		{
			id: "debug",
			source: `---
name: 调试
description: 按「先复现 → 二分 → 改前留证」的流程定位并修复运行时问题
when_to_use: 用户报 bug、异常、报错或说"这个功能坏了"，需要定位根因并修复
---

定位运行问题的流程，按顺序执行，别跳：

1. **先复现**。拿最小输入把问题稳定复现；复现不出来就先收集信息，别急着改。
2. **留证据**。记录报错原文、堆栈、日志片段，以及「此时输入是什么」。
3. **二分定位**：能用注释/临时分支把可疑范围砍半的，先砍半；顺着数据流向源头追。
4. **证明假设再改**。每个猜测都要有对应证据；改前想清楚「它坏了会怎样」。
5. **改后回归**。重跑同一复现路径，确认现象消失且相邻功能没受影响。

容易犯的错：只看表面报错不追调用链、一次性改多个变量、把「可能有问题」当结论、修完不回归。定位到根因后，说明修复方案和影响面再动手。
`,
		},
		{
			id: "testing",
			source: `---
name: 写测试
description: 为改动和修复补齐覆盖，测试断言行为而非复述实现
when_to_use: 用户要求"补测试""加单测""写用例"，或想保证改动不会回归
---

给改动配测试，先想清楚三点：

1. **测行为，不测实现**。断言输入→输出、状态变化、副作用；不要断言某段代码内部长什么样。
2. **正面 + 回归都要**。新功能要有正面断言；被改动的老行为要有回归断言，防止被这次改成坏。
3. **别漏边界**：空值、超长、并发、错误路径。正常路径之外才是大多数 bug 存活的地方。

写法：一个用例断言一件事；优先测公共入口 / 契约；错误路径和成功路径同等重要；依赖外部服务的部分用可替换方式隔离。命名要能读出「在什么条件下期待什么」。写完跑一遍，给出结果与覆盖率概况。
`,
		},
		{
			id: "refactor",
			source: `---
name: 重构
description: 不改变外部行为地整理代码结构，靠测试兜底，小步合入
when_to_use: 用户要求"重构""整理代码""拆分这个大函数 / 文件"，且不希望改变功能
---

重构的铁律：**行为不变**。凡是会改变输出的都不叫重构，那是功能改动。

做法：

1. **先有基线**。跑一遍现有测试或记录现有行为，重构前后据此对照；没有测试兜底的高风险重构要先说清。
2. **小步走**。一次只做一种重构（改名、抽函数、改数据结构……），每步能编译能跑，最好能单独合入。
3. **机械优先**。能用编辑器 / 工具自动完成的（重命名、提取）先做，避免手工引入笔误。
4. **改结构不改语义**。搬动顺序保持行为一致；发现原代码有 bug，停下来单独说明，别混进重构。

过程中一旦发现「好像顺手改了行为」，立刻停下，确认是不是风险信号。重构完跑基线对拍，确认行为一致。
`,
		},
	];
