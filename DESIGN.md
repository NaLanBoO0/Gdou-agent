# 三个自定义功能的设计方案

这份文档讨论「自动化项目 / 专家 / skills」该怎么设计。它是**讨论的输入，不是决定**——
每一节末尾标出需要你拍板的地方。

先说结论：**这三件事不是三个独立功能，是同一个问题的四个正交维度**，而其中一个维度已经
做完了（模式）。如果各自做成一套独立的注册表，会有三份重叠的配置和一个组合爆炸。

---

## 0. 进度

| 功能 | 状态 | 说明 |
|---|---|---|
| 模式 | **已完成** | `src/profiles/`，`general` / `coding` |
| 组合模型 | **已完成** | `src/kernel/recipe.ts`，见 `FEATURES.md` 2.23 |
| 专家 | **已完成** | `src/experts/`，三级加载，只能收窄工具集 |
| skills | 待做 | 挂在本页的组合模型上；按你的决定：只允许说明和资源 |
| 自动化 | 待做 | 复用配方；按你的决定：只做运行时触发，产出单独一个概念 |

第 9 节那四个问题**已经拍板**，答案记在下面各节里。**组合模型已经落地，所以本页第 3 节不再是建议而是既成事实**——skills 和自动化要按它的形状来挂，不要再另起一套。

---

## 1. 先分清各解决什么问题

| 问题 | 谁来回答 | 现状 |
|---|---|---|
| 这个 agent **能**做什么？ | 模式 | 已有：`general` / `coding` |
| 它该**怎么想**？ | 专家 | 待做 |
| 它**需要知道什么**？ | skills | 待做 |
| 它该在**什么时候自己动**？ | 自动化 | 待做 |

这四件事是正交的。同一段对话里可以同时是「coding 模式 + 安全审计专家 + 用了某个 skill」，
而自动化只是把这一组合存下来、再给它一个触发时机。

**正交性不是文字游戏，它决定了要不要新建概念。** 如果专家和模式不能同时生效，那专家就是
模式的一个别名，不该有新名字。

---

## 2. 什么已经有，什么才是真正新的

诚实一点：**「专家」在机制上几乎什么都不新。**

一个专家如果只是"一段更好的系统提示词"，那它就是一个模式。真正新的东西只有三样：

| 功能 | 真正新的机制 |
|---|---|
| 专家 | **组合**——模式和专家可以叠加，而且专家能**收窄**工具集 |
| skills | **渐进式披露**——让 agent 知道某个能力存在，但不把它的全文塞进上下文 |
| 自动化 | **触发与留存**——存下一个意图，在你不看着的时候执行 |

其余都是配置和界面。

**这个判断很重要，因为它决定了工作量在哪。** 比如 skills 的价值不在"能写 markdown"，
而在"只在需要时才把几千字读进上下文"——否则它和我们上一轮刚做的上下文裁剪直接打架：
把所有 skill 全文塞进系统提示，等于把裁剪省下来的预算又花回去。

---

## 3. 组合模型（核心建议）

**一个会话由三部分装配而成，而不是三个并列的系统。**

```ts
/** 现在 */
interface AgentProfile {
  id: string;
  label: string;
  description: string;
  systemPrompt(ctx): string;
  tools(ctx): AnyTool[];
  toolExecution?: "parallel" | "sequential";
  thinkingLevel?: ThinkingLevel;
}

/** 建议：会话按配方装配，而不是直接绑一个 profile */
interface SessionRecipe {
  /** 决定工具集和基础提示。 */
  mode: string;
  /** 决定方法论，并且只能收窄工具集。 */
  expert?: string;
  /** 决定哪些能力可以被按需加载。 */
  skills?: string[];
}

async function createAgent(options: {
  recipe: SessionRecipe;
  // ……其余不变
}): Promise<AgentSession>;
```

装配规则：

```
工具集   = 模式的工具 ∩ 专家声明的允许集
提示词   = 模式的系统提示 + 专家的方法论
可加载   = 配方里列出的 skills（只有名字和一句话描述进入上下文）
```

**专家只能做减法。** 这条是安全属性，不是风格偏好：一个第三方专家包不应该能把
「general 模式」的文件访问打开。允许交集、禁止并集，意味着装一个来路不明的专家最多
让 agent 变笨，不会让它变危险。

---

## 4. 专家

**是什么**：一个有名字的方法论。

**存哪**：`~/.gdou-agent/experts/<id>.md`，frontmatter 放元数据，正文就是提示词。

```markdown
---
name: 安全审计
description: 按攻击面审查代码，不修改任何文件
tools: [read, grep, find, ls]        # 可选，只能收窄
thinkingLevel: high                   # 可选
---

你是一名安全审计员。审查时按以下顺序……
```

用 markdown 而不是代码，是因为用户要能自己写、能分享、能改。一个需要写 TypeScript
才能定制的"专家"，实际使用者只有写这个项目的人。

**怎么选**：每个会话选一个，界面上和模式并排放在头部。切换专家会重建会话
（和切换模式一样），因为系统提示变了。

**和模式的区别**：模式决定**能力**，专家决定**方法**。`coding` 模式给了读文件的工具，
`安全审计` 专家决定怎么读、看什么、按什么顺序。

---

## 5. skills

**是什么**：一份可以按需加载的说明，外加它需要的资源文件。

**存哪**：目录形式，因为 skill 往往不止一个文件。

```
~/.gdou-agent/skills/<name>/SKILL.md     # 用户级
<cwd>/.gdou-agent/skills/<name>/SKILL.md # 项目级，优先
```

```markdown
---
name: 周报生成
description: 把本周的提交整理成周报。当用户要求写周报或总结本周工作时使用。
---

1. 用 bash 取 `git log --since="7 days ago" --oneline`
2. 按模块分组……
（这里可以很长，因为只有被加载时才进入上下文）
```

**机制：渐进式披露。** 这是 skill 唯一真正新的地方，也是它和"把提示词写长一点"的分界。

```
会话开始   →  上下文里只有每个 skill 的名字 + 一句话描述（几十 token）
任务匹配   →  agent 调用 load_skill("周报生成")
           →  正文进入上下文
需要资源   →  agent 用已有的 read 工具去读 skill 目录里的文件
```

如果一开始就把所有 skill 全文塞进系统提示，几十个 skill 就是几十万 token——正好把我们
上一轮花力气做的上下文裁剪抵消掉。**skills 必须建立在按需加载上，否则它和裁剪是矛盾的。**

**工具**：新增两个内核工具，`list_skills` 和 `load_skill`。名字和描述也可以直接进系统提示，
那样连 `list_skills` 都不需要——但会让提示词随 skill 数量增长，所以倾向用工具。

**一个限制**：skill 的资源和脚本依赖文件工具。`general` 模式没有文件访问，所以只能使用
纯说明型的 skill。这是对的——能力边界不该被 skill 绕过。

---

## 6. 自动化

**是什么**：一个存下来的任务 = 配方 + 提示词 + 触发时机。

**存哪**：`~/.gdou-agent/automations/<id>.json`

```jsonc
{
  "name": "每日仓库摘要",
  "recipe": { "mode": "coding", "expert": "代码审查" },
  "prompt": "总结昨天的提交，列出需要我关注的三件事",
  "cwd": "C:/work/some-repo",
  "trigger": { "kind": "schedule", "at": "09:00", "days": ["MO", "TU", "WE", "TH", "FR"] },
  "allowWrite": false
}
```

**产出**：每次运行产生一段普通对话，出现在「历史」里。这样结果不需要另建一套展示——
它就是我们已经有东西，而且可以点进去追问。

**两个必须说清楚的约束**：

1. **桌面应用只在运行时才能触发定时任务。** 关闭程序就不跑。要做到关着也跑，得注册
   Windows 计划任务 / launchd，那是另一个量级的工作，而且平台相关。
   建议第一版**只做"运行时触发"**，并在界面上明说，而不是让用户以为它一直在跑。
2. **无人值守 = 工具在无人值守地运行。** 一个能 `bash` 的定时任务就是一个远程代码执行面。
   建议 `allowWrite` 默认 **false**：自动化默认只读，要放开必须逐条显式打开。

第 2 条和「专家只能收窄」是同一条原则的两次应用：**默认收紧，放开要主动。**

---

## 7. 与现有模式的边界

| | 模式 | 专家 | skill | 自动化 |
|---|---|---|---|---|
| 决定 | 能不能做 | 怎么做 | 知道什么 | 何时做 |
| 粒度 | 会话 | 会话 | 按需 | 独立实体 |
| 能改变工具集吗 | 定义它 | 只能收窄 | 不能 | 不能 |
| 进上下文的方式 | 始终（系统提示） | 始终（拼进提示） | 按需 | — |
| 存储 | 代码 | markdown | 目录 | json |

**一条不能破的规则**：任何用户可安装的东西（专家、skill、自动化）都**不能扩大**能力边界。
只有模式能定义能力，而模式是代码。否则"装个专家"就变成了"装个后门"。

---

## 8. 建议的落地顺序

不是按"哪个最小"，是按依赖：

1. **专家** —— 先做它，因为它逼我们把**组合模型**定下来。skills 和自动化都要挂在这个
   组合上；先建错地基再盖三层楼是最贵的错误。而且它本身工作量小，试错便宜。
2. **skills** —— 渐进式披露是三个里唯一真正新的机制，值得单独一轮。它还需要新增内核工具，
   会动到工具层。
3. **自动化** —— 它本身就是"配方 + 提示词 + 触发"，前两样必须先存在。再叠上调度和
   无人值守的安全策略。

---

## 9. 已拍板的问题

这四个是产品决定，已经定了，记在这里以免以后重新讨论：

1. **专家要不要随包提供？** → **提供几个简单的**。已内置三个：`security-audit`（收窄到只读）、`code-review`（只读 + bash，`thinkingLevel: high`）、`researcher`（不收窄，任何模式可用）。前两个演示"收窄"，第三个演示"不收窄"。
2. **skill 能不能带可执行脚本？** → **第一版只允许说明和资源文件**，不允许脚本。安装一个 skill 不等于安装一段可执行代码。
3. **自动化要不要支持"关着也跑"？** → **第一版只做运行时触发**，界面上要明说，而不是让用户以为它一直在跑。
4. **自动化的产出放哪？** → **单独一个新概念**，不混进「历史」。

第 2、3 条和「专家只能收窄」是同一条原则的三次应用：**默认收紧，放开要主动。**

---

## 附：这份方案刻意不做的事

- **不做插件市场。** 分发、签名、审核、版本冲突都是独立的大问题，和"让 agent 更好用"不是
  一回事。先把机制做对。
- **不让 skill 定义新工具。** 那需要一套工具注册与权限模型，而且会让"能力边界只由模式决定"
  这条规则失效。
- **不给自动化做可视化编排。** 拖拽式工作流是另一个产品。这里的自动化就是"一段存下来的
  提示词 + 一个触发时机"。
